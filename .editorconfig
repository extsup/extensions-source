// main.js - Ganti fungsi hideLoginForm() yang sudah adafunction hideLoginForm() {    document.getElementById('loginForm').style.display = 'none';    // Menggunakan ID baru: 'main'    document.getElementById('main').style.display = 'block';     // Tampilkan header dan bottom nav yang baru    document.getElementById('appHeader').style.display = 'flex';     document.getElementById('bottomNav').style.display = 'flex'; }// main.js - Ganti fungsi showLoginForm() yang sudah adafunction showLoginForm() {    document.getElementById('loginForm').style.display = 'block';    // Menggunakan ID baru: 'main'    document.getElementById('main').style.display = 'none';     // Sembunyikan header dan bottom nav saat di form login    document.getElementById('appHeader').style.display = 'none';     document.getElementById('bottomNav').style.display = 'none';     clearToken();}// main.js - Ganti fungsi showDefaultHome() yang sudah adafunction showDefaultHome() {    const token = localStorage.getItem('ghToken');        if (!token) {        showLoginForm();        return;    }        // Hapus blok pencarian di sini, karena sudah dipindah ke header/bottom nav    // Serta fokus pada tombol aksi cepat dan informasi    updateContent('<h3>🏠 Home</h3>' +        '<div class="file-actions">' +        // showMyWorkflows dan showRecentSessions ada di workflows.js dan sessions.js        '<button onclick="showMyRepos()">📂 Repository Saya</button>' +        '<button onclick="showMyIssues()">🐛 Issues Saya</button>' +        '<button onclick="showMyWorkflows()">🚀 Workflow Saya</button>' +        '<button onclick="showRecentSessions()">⏳ Sesi Terakhir</button>' +        '</div>' +        '<div id="sessionsList" class="session-container"></div>'); // Container untuk sesi aktif/terakhir        // Muat sesi aktif/terakhir (fungsi dari sessions.js)    loadActiveSessions();        // Pastikan item nav yang aktif disetel    document.querySelectorAll('#bottomNav .nav-item').forEach(item => item.classList.remove('active'));    document.querySelector('#bottomNav .nav-item:nth-child(1)').classList.add('active'); }